# Solar system phase — controls the Solar widget state
# Options: signed | permitted | installed | live
SOLAR_PHASE=live

# Steam Web API key — enables library stats in SteamWidget (build time)
STEAM_API_KEY=

# Tesla credentials are NOT env vars here — they live as Cloudflare Worker
# secrets (worker/wrangler.toml) with tokens stored in Workers KV.
